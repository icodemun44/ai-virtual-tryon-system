// apps/web-dashboard/vite.config.js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173, // Lock the web dashboard to its standard local port
    proxy: {
      // Intercept any frontend requests starting with '/api' 
      // and securely route them to your running FastAPI server
      '/api': {
        target: 'http://127.0.0.1:8000',
        changeOrigin: true,
        secure: false,
      }
    }
  }
})